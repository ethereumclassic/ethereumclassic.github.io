import React from 'react';

import LayoutWithMenu from './LayoutWithMenu';

const LayoutIndex = props => {
  return <LayoutWithMenu {...props}>I am an index page!</LayoutWithMenu>;
};

export default LayoutIndex;
