dist: trusty
sudo: false
language: node_js
node_js:
- 7
install:
  - npm install --no-optional
script: travis_wait bash ./deploy.sh
env:
  global:
  - ENCRYPTION_LABEL: "a55c8da1c39b"
  - SOURCE_BRANCH: "source"
  - TARGET_BRANCH: "master"
cache:
  directories:
    - node_modules